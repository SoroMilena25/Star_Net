{% extends 'base.html.twig' %}

{% block title %}Profil de {{ user.pseudo }}{% endblock %}

{% block body %}
    <h1>{{ user.nom }} {{ user.prenom }}</h1>
    <p>Pseudo: {{ user.pseudo }}</p>
    <p>Email: {{ user.email }}</p>

    /*<h2>Abonnements:</h2>
    <ul>
        {% for abonnement in abonnements %}
            <li>{{ abonnement.a_qui.pseudo }}</li>
        {% else %}
            <li>Aucun abonnement.</li>
        {% endfor %}
    </ul>

    <h2>Abonnés:</h2>
    <ul>
        {% for abonne in abonnees %}
            <li>{{ abonne.qui.pseudo }}</li>
        {% else %}
            <li>Aucun abonné.</li>
        {% endfor %}
    </ul>

    <h2>Publications:</h2>
    <ul>
        {% for publication in publications %}
            <li>{{ publication.contenu }}</li> 
        {% else %}
            <li>Aucune publication.</li>
        {% endfor %}
    </ul>
{% endblock %}
